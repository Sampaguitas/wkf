version: "3.3"
services:
  nginx:
    depends_on:
      - user
      - drop
      - update
    build:
      context: ./nginx
      dockerfile: Dockerfile
    restart: always
    ports:
      - '80:80'
  user:
    build:
      context: ./user
      dockerfile: Dockerfile.dev
    restart: on-failure
    container_name: user
    volumes:
      - /app/node_modules
      - "./user/src:/app/src"
    env_file:
      - ./env/backend.env
  drop:
    build:
      context: ./drop
      dockerfile: Dockerfile.dev
    restart: on-failure
    container_name: drop
    volumes:
      - /app/node_modules
      - "./drop/src:/app/src"
    env_file: 
      - ./env/backend.env
  update:
    build:
      context: ./update
      dockerfile: Dockerfile.dev
    restart: on-failure
    container_name: update
    volumes:
      - /app/node_modules
      - "./update/src:/app/src"
    env_file: 
      - ./env/backend.env